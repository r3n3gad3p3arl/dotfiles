(defwindow osd
   :monitor 0
   :stacking "fg"
   :focusable false
   :geometry (geometry
      :x "0%"
      :y "8px"
      :width "200px"
      :height "20px"
      :anchor "top center")
   (volume))

(defwidget osd []
   (overlay
      (progress :orientation "h" :value {osd.value})
      (label :text {osd.icon} :halign "center")))

(defvar osd "{ \"icon\": \"󰖀\", \"value\": 0 }")
